<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.attafakkur.moviedbpro">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieDBPro"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.home.FavoriteActivity"/>
        <activity android:name=".ui.home.SearchActivity" />
        <activity android:name=".ui.home.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.detail.DetailTVActivity" />
        <activity android:name=".ui.detail.DetailMoviesActivity" />
        <activity android:name=".ui.detail.DetailTrendingActivity" />
        <activity android:name=".ui.detail.DetailSearchActivity" />
        <activity
            android:name=".ui.home.HomeActivity"
            android:configChanges="orientation|screenSize" />
    </application>

</manifest>